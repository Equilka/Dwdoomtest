package com.equilka.discordbot.data.downloadable;

import java.io.File;

public class AudioData extends Downloadable {
    public AudioData(String title, String link, File file, String mode) {
        super(title, link, file, mode);
    }
}
